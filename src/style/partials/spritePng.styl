/*
Stylus variables are information about icon's compiled state, stored under its original file name

.icon-home {
  width: $icon_home_width;
}

The large array-like variables contain all information about a single icon
$icon_home = x y offset_x offset_y width height total_width total_height image_path;

At the bottom of this section, we provide information about the spritesheet itself
$spritesheet = width height image $spritesheet_sprites;
*/
$busket_name = 'busket';
$busket_x = 80px;
$busket_y = 0px;
$busket_offset_x = -80px;
$busket_offset_y = 0px;
$busket_width = 57px;
$busket_height = 56px;
$busket_total_width = 172px;
$busket_total_height = 118px;
$busket_image = '../img/spritePng.png';
$busket = 80px 0px -80px 0px 57px 56px 172px 118px '../img/spritePng.png' 'busket';
$iconbasket_name = 'iconbasket';
$iconbasket_x = 80px;
$iconbasket_y = 56px;
$iconbasket_offset_x = -80px;
$iconbasket_offset_y = -56px;
$iconbasket_width = 35px;
$iconbasket_height = 35px;
$iconbasket_total_width = 172px;
$iconbasket_total_height = 118px;
$iconbasket_image = '../img/spritePng.png';
$iconbasket = 80px 56px -80px -56px 35px 35px 172px 118px '../img/spritePng.png' 'iconbasket';
$iconphone_name = 'iconphone';
$iconphone_x = 137px;
$iconphone_y = 0px;
$iconphone_offset_x = -137px;
$iconphone_offset_y = 0px;
$iconphone_width = 35px;
$iconphone_height = 35px;
$iconphone_total_width = 172px;
$iconphone_total_height = 118px;
$iconphone_image = '../img/spritePng.png';
$iconphone = 137px 0px -137px 0px 35px 35px 172px 118px '../img/spritePng.png' 'iconphone';
$logo_name = 'logo';
$logo_x = 0px;
$logo_y = 0px;
$logo_offset_x = 0px;
$logo_offset_y = 0px;
$logo_width = 80px;
$logo_height = 118px;
$logo_total_width = 172px;
$logo_total_height = 118px;
$logo_image = '../img/spritePng.png';
$logo = 0px 0px 0px 0px 80px 118px 172px 118px '../img/spritePng.png' 'logo';
$spritesheet_width = 172px;
$spritesheet_height = 118px;
$spritesheet_image = '../img/spritePng.png';
$spritesheet_sprites = $busket $iconbasket $iconphone $logo;
$spritesheet = 172px 118px '../img/spritePng.png' $spritesheet_sprites;

/*
The provided mixins are intended to be used with the array-like variables

.icon-home {
  spriteWidth($icon_home)
}

.icon-email {
  sprite($icon_email)
}
*/
spriteWidth($sprite) {
  width: $sprite[4];
}

spriteHeight($sprite) {
  height: $sprite[5];
}

spritePosition($sprite) {
  background-position: $sprite[2] $sprite[3];
}

spriteImage($sprite) {
  background-image: url($sprite[8]);
}

sprite($sprite) {
  spriteImage($sprite)
  spritePosition($sprite)
  spriteWidth($sprite)
  spriteHeight($sprite)
}

/*
The `sprites` mixin generates identical output to the CSS template
  but can be overridden inside of Stylus

This must be run when you have at least 2 sprites.
  If run with a single sprite, then there will be reference errors.

sprites($spritesheet_sprites);
*/
sprites($sprites) {
  for $sprite in $sprites {
    $sprite_name = $sprite[9];
    .{$sprite_name} {
      sprite($sprite);
    }
  }
}
